{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window (Phase 0 — core only).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
